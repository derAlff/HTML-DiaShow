// === Slideshow Configuration ===
const SLIDESHOW_CONFIG = {
    SRC_DIR: './src/',          // Folder with pictures
    SLIDE_MS: 5000,             // Duration per picture (in ms)
    POLL_MS: 5000,              // Polling-Intervall (in ms)
    EXTS: ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.bmp']
  };